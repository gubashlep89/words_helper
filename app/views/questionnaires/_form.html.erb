<%= simple_form_for(@questionnaire) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :description %>

    <h3>Вопросы</h3>
        <div id='questions'>
    <%= f.simple_fields_for :questions do |question| %>
    <%= render 'question_fields', :f => question %>
    <% end %>
          <div class='links'>
    <%= link_to_add_association 'Добавить вопрос', f, :questions %>
          </div>
        </div>
    <%#= f.simple_fields_for :questions do |question_f| %>
      <%#= question_f.input :question_text, label: 'Вопрос' %>
<!--      <%#= question_f.input :correct_answer, as: :select,-->
                           collection: 1..4, label: 'Правильный ответ' %>
<!--      <ul>-->
        <%#= question_f.simple_fields_for :question_answers do |answer_f| %>
<!--          <li>-->
            <%#= answer_f.input :answer_text, label: 'Отыет' %>
<!--          </li>-->
        <%# end %>
<!--      </ul>-->
    <%# end %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
